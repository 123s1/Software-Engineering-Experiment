# 实验3：数据表汇总与关系说明

## 1. 文档说明

本文件将实验3的数据库设计内容整理成**便于写实验报告的表格形式**，便于直接填入实验报告。

## 2. 数据表总览

| 序号 | 表名 | 中文名称 | 类别 | 主要作用 | 对应实验2的类 |
| --- | --- | --- | --- | --- | --- |
| 1 | t_user | 用户表 | 主表 | 存储管理员和师生用户信息 | User / Administrator / TeacherStudentUser |
| 2 | t_classroom | 教室表 | 主表 | 存储教室资源信息 | Classroom |
| 3 | t_classroom_time_slot | 教室可用时间段表 | 从属表 | 存储教室在某天某课时段的可用情况 | ClassroomTimeSlot |
| 4 | t_reservation | 预订记录表 | 中间表 | 拆分师生与教室的 N:M 预订关系 | Reservation |
| 5 | t_classroom_mgmt_record | 教室管理记录表 | 中间表 | 拆分管理员与教室的 N:M 管理关系 | ClassroomManagementRecord |
| 6 | t_user_mgmt_record | 用户管理记录表 | 中间表 | 拆分管理员与用户的 N:M 管理关系 | UserManagementRecord |

## 3. 各表字段明细

### 3.1 用户表（t_user）

| 字段名 | 数据类型 | 主/外键 | 允许空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| user_id | VARCHAR(32) | PK | 否 | - | 用户唯一编号 |
| account | VARCHAR(50) | UK | 否 | - | 登录账号（唯一） |
| user_name | VARCHAR(50) | - | 否 | - | 用户姓名 |
| password | VARCHAR(128) | - | 否 | - | 密码（加密存储） |
| role | VARCHAR(20) | - | 否 | - | 角色：ADMIN / TEACHER / STUDENT |
| identity_type | VARCHAR(20) | - | 是 | NULL | 身份类型（仅师生有值） |
| contact_info | VARCHAR(100) | - | 是 | NULL | 联系方式 |
| user_status | VARCHAR(20) | - | 否 | ACTIVE | 状态：ACTIVE / DISABLED |

### 3.2 教室表（t_classroom）

| 字段名 | 数据类型 | 主/外键 | 允许空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| classroom_id | VARCHAR(32) | PK | 否 | - | 教室唯一编号 |
| classroom_name | VARCHAR(50) | - | 否 | - | 教室名称 |
| location | VARCHAR(100) | - | 否 | - | 教室位置 |
| capacity | INT | - | 否 | - | 教室容量（座位数） |
| equipment_info | VARCHAR(200) | - | 是 | NULL | 设备信息 |
| classroom_status | VARCHAR(20) | - | 否 | AVAILABLE | 状态：AVAILABLE / DISABLED |

### 3.3 教室可用时间段表（t_classroom_time_slot）

| 字段名 | 数据类型 | 主/外键 | 允许空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| slot_id | VARCHAR(32) | PK | 否 | - | 时间段唯一编号 |
| classroom_id | VARCHAR(32) | FK → t_classroom | 否 | - | 所属教室编号 |
| use_date | DATE | - | 否 | - | 使用日期 |
| start_section | INT | - | 否 | - | 开始课时 |
| end_section | INT | - | 否 | - | 结束课时 |
| slot_status | VARCHAR(20) | - | 否 | FREE | 状态：FREE / RESERVED / UNAVAILABLE |

### 3.4 预订记录表（t_reservation）

| 字段名 | 数据类型 | 主/外键 | 允许空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| reservation_id | VARCHAR(32) | PK | 否 | - | 预订记录唯一编号 |
| user_id | VARCHAR(32) | FK → t_user | 否 | - | 申请人编号 |
| classroom_id | VARCHAR(32) | FK → t_classroom | 否 | - | 被预订教室编号 |
| slot_id | VARCHAR(32) | FK → t_classroom_time_slot | 否 | - | 占用时间段编号 |
| purpose | VARCHAR(200) | - | 是 | NULL | 预订用途 |
| reservation_status | VARCHAR(20) | - | 否 | CREATED | 状态：CREATED / CANCELLED / FINISHED |
| create_time | DATETIME | - | 否 | - | 预订创建时间 |

### 3.5 教室管理记录表（t_classroom_mgmt_record）

| 字段名 | 数据类型 | 主/外键 | 允许空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| record_id | VARCHAR(32) | PK | 否 | - | 管理记录唯一编号 |
| operator_id | VARCHAR(32) | FK → t_user | 否 | - | 操作人（管理员）编号 |
| classroom_id | VARCHAR(32) | FK → t_classroom | 否 | - | 被操作教室编号 |
| operation_type | VARCHAR(20) | - | 否 | - | 操作类型：ADD / DELETE / SET_TIMESLOT |
| operation_time | DATETIME | - | 否 | - | 操作时间 |
| remarks | VARCHAR(500) | - | 是 | NULL | 备注信息 |

### 3.6 用户管理记录表（t_user_mgmt_record）

| 字段名 | 数据类型 | 主/外键 | 允许空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| record_id | VARCHAR(32) | PK | 否 | - | 管理记录唯一编号 |
| operator_id | VARCHAR(32) | FK → t_user | 否 | - | 操作人（管理员）编号 |
| target_user_id | VARCHAR(32) | FK → t_user | 否 | - | 被操作目标用户编号 |
| operation_type | VARCHAR(20) | - | 否 | - | 操作类型：ENABLE / DISABLE |
| operation_time | DATETIME | - | 否 | - | 操作时间 |
| remarks | VARCHAR(500) | - | 是 | NULL | 备注信息 |

## 4. 主外键关系汇总表

| 序号 | 关系类型 | 外键所在表 | 外键字段 | 引用表 | 引用字段 | 多重性 | 说明 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 从属 | t_classroom_time_slot | classroom_id | t_classroom | classroom_id | 1:N | 教室包含多个时间段 |
| 2 | 关联 | t_reservation | user_id | t_user | user_id | 1:N | 用户发起多条预订 |
| 3 | 关联 | t_reservation | classroom_id | t_classroom | classroom_id | 1:N | 教室对应多条预订 |
| 4 | 关联 | t_reservation | slot_id | t_classroom_time_slot | slot_id | 1:N | 时间段对应预订 |
| 5 | 关联 | t_classroom_mgmt_record | operator_id | t_user | user_id | 1:N | 管理员操作教室 |
| 6 | 关联 | t_classroom_mgmt_record | classroom_id | t_classroom | classroom_id | 1:N | 教室被管理 |
| 7 | 关联 | t_user_mgmt_record | operator_id | t_user | user_id | 1:N | 管理员管理用户 |
| 8 | 关联 | t_user_mgmt_record | target_user_id | t_user | user_id | 1:N | 用户被管理 |

## 5. 三组多对多关系拆分说明

### 5.1 师生 ↔ 教室（预订关系 N:M）

**原始关系**：多名师生可以预订多间教室

**拆分方案**：通过 `t_reservation` 表拆分

| 一对多关系1 | 一对多关系2 |
| --- | --- |
| t_user (1) → (0..*) t_reservation | t_reservation (0..*) → (1) t_classroom |
| 一个师生用户可发起多条预订 | 一条预订对应一个目标教室 |

### 5.2 管理员 ↔ 教室（管理关系 N:M）

**原始关系**：多个管理员可以管理多个教室（增加、删除、设置时段）

**拆分方案**：通过 `t_classroom_mgmt_record` 表拆分

| 一对多关系1 | 一对多关系2 |
| --- | --- |
| t_user[管理员] (1) → (0..*) t_classroom_mgmt_record | t_classroom_mgmt_record (0..*) → (1) t_classroom |
| 一个管理员可产生多条教室管理记录 | 一条管理记录对应一个教室 |

### 5.3 管理员 ↔ 用户（管理关系 N:M）

**原始关系**：多个管理员可以管理多名师生用户（启用、禁用）

**拆分方案**：通过 `t_user_mgmt_record` 表拆分

| 一对多关系1 | 一对多关系2 |
| --- | --- |
| t_user[管理员] (1) → (0..*) t_user_mgmt_record | t_user_mgmt_record (0..*) → (1) t_user[目标用户] |
| 一个管理员可产生多条用户管理记录 | 一条管理记录对应一个目标用户 |

## 6. 索引设计汇总

| 表名 | 索引字段 | 索引目的 |
| --- | --- | --- |
| t_user | role | 按角色筛选用户 |
| t_user | user_status | 按状态筛选用户 |
| t_classroom | classroom_status | 按状态筛选教室 |
| t_classroom_time_slot | (classroom_id, use_date) | 按教室和日期查询时间段 |
| t_classroom_time_slot | slot_status | 按状态筛选空闲时段 |
| t_reservation | user_id | 按用户查询预订 |
| t_reservation | (classroom_id, slot_id) | 按教室和时段查询预订 |
| t_reservation | reservation_status | 按预订状态筛选 |
| t_reservation | create_time | 按创建时间排序 |
| t_classroom_mgmt_record | operator_id | 按管理员查询操作记录 |
| t_classroom_mgmt_record | classroom_id | 按教室查询管理历史 |
| t_classroom_mgmt_record | operation_time | 按操作时间排序 |
| t_user_mgmt_record | operator_id | 按管理员查询操作记录 |
| t_user_mgmt_record | target_user_id | 按目标用户查询管理历史 |
| t_user_mgmt_record | operation_time | 按操作时间排序 |
